<h1>Lista de Postagens:</h1>

{{#each posts}}

    <h1>{{titulo}}</h1>
    <p>{{conteudo}}</p>
    <small>{{createdAt}}</small>
    <a href=" /deletar/{{dataValues.id}}"><button>Deletar</button></a>
    <a href=" /edit/{{dataValues.id}}"><button>Editar</button></a>
    <hr>

{{/each}}

    <a href=" /cad"><button>Nova Postagem</button></a>